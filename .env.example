# WPConvert API credentials (fake values — replace with your own)
# The CLI reads WPCONVERT_API_KEY from the environment or ~/.wpconvert/config.json.
# The MCP server requires WPCONVERT_API_KEY in its environment.

WPCONVERT_API_KEY=wpc_live_xxxxxxxxxxxxxxxxxxxxxxxx
WPCONVERT_API_BASE=https://api.wpconvert.ai
